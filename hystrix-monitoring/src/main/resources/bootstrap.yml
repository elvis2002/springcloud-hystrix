eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry:8761/eureka/

spring:
  application:
    name: hystrix-monitor
  rabbitmq:
    host: rabbitmq